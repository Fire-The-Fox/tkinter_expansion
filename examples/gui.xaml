<?xml version="1.0" encoding="utf-8" ?>
<gui>
    <root element="Tk()">
        <configure>"width=700, height=700"</configure>
    </root>
    <button element="Button(root, text='Generated with xml')">
        <place>"x=250, y=25"</place>
        <configure>"bg='#3c9e31', bd=0, activebackground='#3c9e31', relief=tk.SUNKEN"</configure>
        <bind element="Button-3">"lambda event: designer.select_widget(event)"</bind>
    </button>
    <Label element="Label(root, text='xml is pog!', font=('Calibri', 25))">
        <place>"anchor=tk.CENTER, relx=0.5, y=100"</place>
    </Label>
    <Label3 element="Label(root, text='xml is cool!', font=('Calibri', 25))">
        <place>"anchor=tk.CENTER, relx=0.5, y=135"</place>
    </Label3>
</gui>